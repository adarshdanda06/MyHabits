'use client'

import { useState } from "react"

export default function NavbarItems() {
    const [active, setActive] = useState(false)

    return (
        <div>
            <div></div>
        </div>
    )
}